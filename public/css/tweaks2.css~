.above {
    z-index: 2;
}

.wif {
	position:fixed;
	top:25px;
	right:150px;
	}
body {
background:white;
}
pre {
   background-color: ghostwhite;
   border: 1px solid silver;
   padding: 10px 20px;
   margin: 20px; 
   }
.json-key {
   color: brown;
   }
.json-value {
   color: navy;
   }
.json-string {
   color: olive;
   }


.warpColorChoose {
    position: absolute;
    right: 165px;
}
.weftColorChoose {
    position: absolute;
    right: 100px;
}
.setColor {
    position: absolute;
    right: -30px;
}

.base {
    z-index: 1;
}


.introBox{
    position: fixed;
    background-image: url("/img/background.png");
    background-repeat: repeat;
    opacity: 0.8;
    filter: alpha(opacity=40);
    text-align:center;
    padding: 20px;
    top: 80px;
    overflow: auto;
    direction:rtl;
    bottom: 0px;
    right: 0px;
    left: 0px;
   -webkit-box-shadow: inset 0px -4px 13px 0px rgba(0,0,0,0.33);
	-moz-box-shadow: inset 0px -4px 13px 0px rgba(0,0,0,0.33);
	box-shadow: inset 0px -4px 13px 0px rgba(0,0,0,0.33);
    }
.borderTryBox {
    margin-top: 200px;
    position: fixed;
    background:white;
    text-align:center;
    padding: 20px;
    top: 30px;
    overflow: auto;
    direction:rtl;
    bottom: 20px;
    right: 20px;
    left: 20px;
    -webkit-box-shadow: 10px 10px 24px 0px rgba(143, 136, 143, 0.63);
    -moz-box-shadow: 10px 10px 24px 0px rgba(143, 136, 143, 0.63);
    box-shadow: 10px 10px 24px 0px rgba(143, 136, 143, 0.63);
}
.tryBox {
    position: relative;
    top:20px;
    height:100%;
    display:inline-block;
}
.tieUpNum{
	position:absolute;
	top:-50px;
}
.warpColorGrid {
    position: absolute;
    direction: rtl;
    right: 0px;
    height: 32px;
}
.setWarpsAndWefts {
    margin-top: 10px;
    height: 30px;
    width: 40px;
}
.weftColorGrid {
    position: absolute;
    overflow: auto;
    right: 230px;
    width: 35px;
}
.setNumbers {
    position: fixed;
    top: 20px;
    left: 20px;
}
.weaveBoxBorder {
    position: absolute;
    direction: rtl;
    
  
    right: 285px;
}
#weaveBox {
    position: absolute;
    top: 120px;
    height:100%;
    width:100%;
}
.choose {
    margin-top: 20px;
}
.hGrid {
    position: absolute;
    top: 0px;
    right: 0px;
    direction: rtl;
}
.hGridNum {
    position: absolute;
    top: -50px;
    left: 0px;
    direction: rtl;
}
.activeButton {
    background: #444;
}
